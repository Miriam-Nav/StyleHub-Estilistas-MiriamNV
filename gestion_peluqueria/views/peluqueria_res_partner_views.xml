<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_peluqueria_res_partner_kanban" model="ir.ui.view">
        <field name="name">res.partner.kanban.peluqueria</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="display_name"/>
                <field name="image_128"/>
                <field name="phone"/>
                <field name="category_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('res.partner', 'image_128', record.id.raw_value)" 
                                    class="o_image_64_cover"/>
                            </div>
                            <div class="oe_kanban_details">
                                <strong>
                                    <field name="display_name"/>
                                </strong>
                                <ul>
                                    <li t-if="record.phone.raw_value">
                                        <i class="fa fa-phone me-2"/> 
                                        <field name="phone"/>
                                    </li>
                                    <li>
                                        <field name="category_id" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>